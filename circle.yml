machine:
  services:
    - docker
dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - docker build -t juokaz/pyhp .
test:
  override:
    - make flake8
    - make tests-cov
    - make build-nojit
